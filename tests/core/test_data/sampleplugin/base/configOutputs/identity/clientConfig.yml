---
# Key/value pairs to be added to the game's awsGameKitClientConfig.yml file
# These values will be replaced at the end of create/update of
# the feature's CloudFormation stack.

user_pool_client_id: "{{AWSGAMEKIT::CFNOUTPUT::GameKitUserPoolClientId}}"
identity_api_gateway_base_url: "{{AWSGAMEKIT::CFNOUTPUT::IdentityApiGatewayBaseUrl}}"
identity_region: "{{AWSGAMEKIT::CFNOUTPUT::IdentityRegion}}"
